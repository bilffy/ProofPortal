<div class="mt-3">
</div>
<div class="alert alert-danger">
    @if(isset($message))
        {!! $message !!} 
    @else
        Sorry, you are not authorised to access that location. 
    @endif
</div>